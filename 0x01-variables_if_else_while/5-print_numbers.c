#include <stdio.h>

/**
 * main - Displays all single digit of base 10 from 0
 *
 * Always: Return(Success)
 *
 */
int main(void)
{
	int num;
	for (num = 0; num < 10; num++)
	{
		printf("%d", num);
	}
	printf("\n");
	return (0);
}
